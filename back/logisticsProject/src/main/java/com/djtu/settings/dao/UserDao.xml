<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.djtu.settings.dao.UserDao">

    <insert id="addUser">
        insert into tbl_user value (
            #{username},
            #{password},
            #{name},

            )
    </insert>

    <select id="getUserByName" resultType="com.djtu.settings.pojo.User">
        SELECT id,
               username,
               password,
               salt,
               name,
               sex,
               ident,
               ident_id
        from tbl_user
        where username = #{username}
    </select>

</mapper>
