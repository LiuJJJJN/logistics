<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.djtu.settings.dao.ReplyDao">

    <cache eviction="FIFO"
           flushInterval="60000"
           size="512"
           readOnly="true"/>

    <select id="getReplyByFeedbackId" resultType="com.djtu.settings.pojo.Reply">
        select id,
               reply,
               tutor_id,
               feedback_id,
               time
        from tbl_reply
        where feedback_id=#{feedbackId}
    </select>

</mapper>
